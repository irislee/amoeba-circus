<%= form_for(@act) do |f| %>
  <% if @act.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@act.errors.count, "error") %> prohibited this act from being saved:</h2>

      <ul>
      <% @act.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :performance_date %><br>
    <%= f.datetime_select :performance_date %>
  </div>
  <div class="field">
    <%= f.label :amoebas %><br>
    <%= check_box_tag 'acts[amoebas][]', amoeba.id, false, :id => amoeba.name %>
    <%#= f.collection_check_boxes(:amoeba_ids, @amoebas, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>